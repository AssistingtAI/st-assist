# 2026-09-11 工作日志

## 每周五保壳风云榜自动更新（automation-1782198499699）

### 执行结果：数据链成功，GitHub 发布受阻

**1. weekly_update_friday.py 13步全量更新 — 成功**
- 覆盖 207 家 ST/*ST 公司，行情/价格/市值均 207/207 采集成功
- V2十二维评级分布：**A20 / B88 / C60 / D39**，全市场均分 **51.9**（国资壳53家53.8 / 民企壳136家51.7）
- 名单变动：**新增 2 只**（002743 富煌钢构、301117 佳缘科技）；**移除 2 只**（000711 京蓝、002586 围海 —— 上周核验已确认东财板块残留，本次如期剔除）
- 上交所风险警示板页面返回 404（脚本容错，改用国证 ST 板块名单 SH994429，解析 204 只成分股 + 保留 3 只深B股）
- V2 Top1：600965 *ST福成 85分 A；V2 Bottom：002717 *ST岭南 19.4分 D
- 莫高 600543：63.6分/B（C1=5 C2=10 S1=9.6 S2=6 A1=9 A2=3 A3=0 D1=0 B1=7 B2=12 F1=2 H1=0）

**2. verify_html.js 验证 — 全部通过**
- RAW rows 207 | V2 rows 207 | V2 row len 21
- RAW bad rows 0 | V2 sum!=total **0** | V2 missing codes 0
- JS syntax: OK；莫高 sum=total=63.6 level=B 一致

**3. git commit — 成功**（本地）
- commit `5139e03`，13 files changed，+10872/-10843
- 变更文件：baokeng-rank.html、index.html、st_names/market_data/scores/scores_v2/controllers/pledges/trends/deduct_income.json、market_cap.json、_check.js、weekly_changes.json

**4. git push origin HEAD — 失败（阻断）**
- **根因1（网络）**：github.com:443 完全不通（TCP443_FAIL），三次重试均 `Failed to connect to server`；DNS 正常解析 20.205.243.166
- **根因2（凭据）**：Windows 凭据管理器**无 github.com 条目**（仅有 gitee.com/soccers），`git credential fill` 挂起等交互输入
- **对比验证**：api.github.com 可达(200)、baidu 200、codeload 301 → 仅 github.com:443 被阻断
- 无 gh CLI、无 GITHUB_TOKEN/GH_TOKEN 环境变量、无 .git-credentials、无代理配置
- **按指令停止发布**，未生成不完整页面（HTML 本身完整且已验证，仅未上线）

### 待办 / 下次注意
- GitHub Pages 尚未更新，线上仍为上一版本（https://zsheng007.github.io/baokeng-ranking/）
- 需恢复手段之一：① 配置可用代理/VPN 后 `git push origin HEAD`；② 补 GitHub PAT 到凭据管理器后用 Git Data API（blobs→trees→commits→refs）推送，api.github.com 是本机可达的
- 本地 commit 5139e03 已就绪，恢复网络后直接 push 即可，无需重跑数据链
- 上周 11:00 核验 automation 提示的 9:00 更新未生效问题，本次已通过手动执行完整刷新解决（st_names.json 已更新至 2026-09-11）

## ✅ 补记：账号更名 + 推送落地（11:00-11:20）

**账号更名**：老Z把 GitHub 账号 Zsheng007 → **AssistingtAI**（注意拼写是 Assisting**t**AI，此前口头说 AssistingAi 与实际不符，靠 token `GET /user` 才确认）
- 新 Pages：`https://assistingtai.github.io/baokeng-ranking/`（仓库名仍 baokeng-ranking，改名计划作废）
- 旧 `zsheng007.github.io` **404，不重定向** → 公众号历史链接需替换
- 同步 4 处引用：fetch_market_cap.py / generate_html.py / 公众号文案 / 项目记忆；git remote set-url 到新地址

**推送过程（三关）**：
1. **PAT 权限坑**：两个 fine-grained PAT 都只有读（blob 创建 403 `Resource not accessible by personal access token`）→ 换 **classic token（scope=repo）** 成功。教训：fine-grained 必须显式勾 Contents:Read and write
2. **git 通道故障**：首次 push 被 SIGTERM；后续 github.com:443 `CONNECT tunnel failed 502`／`schannel handshake failed`，而 api.github.com 一直 200
3. **解法**：改用 **Git Data API 链式推送到远端** —— 新工具 `push_via_api.py`（项目根目录，可复用）
   - 第一版取工作区原始字节 → 14 个文件 CRLF 与 git 对象 LF 不一致，tree 对不上 → 改为 `git cat-file blob` 取规范化内容后 **tree 完全匹配**
   - 共重放 2 个提交（本地领先远端）：`5139e03`（周五自动更新）→ 远端 `b1ac3bf5`；`404a74d`（账号更名）→ 远端 `28638b2a`
   - 远端 main 最终 = `28638b2a`，tree=`87a7465e` 与本地一致

**线上核验（assistingtai.github.io/baokeng-ranking）**：HTTP 200 / 十二维×4 / 十三维残留0 / 更新时间 2026-09-11 / 覆盖207家 / 冀凯12维 `5,10,10,4,9,6,0,0,10,12,0,4`=70 → 周五自动更新数据已上线

**遗留**：本地 main 与远端 SHA 分叉（内容树一致）；下次推送前需 `git fetch && git reset --hard origin/main` 对齐，或继续用 push_via_api.py。github.com:443 连通性待恢复。
